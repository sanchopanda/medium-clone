const state = {
    isSubmiting: false
}

const mutations = {
    registerStart(state) {
        state.isSubmiting = true
    }
}

const actions = {
    register(context) {

    }
}

export default {
    state,
    mutations,
    actions
}
